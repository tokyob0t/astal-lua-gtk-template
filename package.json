{
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix",
    "build": "meson setup --prefix \"$(pwd)/dist\" build --wipe ; meson install -C build",
    "dev": "pnpm run build && GSETTINGS_SCHEMA_DIR=\"$(pwd)/dist/share/glib-2.0/schemas\" ./dist/bin/*"
  },
  "devDependencies": {
    "@eslint/js": "latest",
    "@girs/adw-1": "latest",
    "esbuild": "latest",
    "eslint": "latest",
    "typescript": "latest",
    "typescript-eslint": "latest"
  },
  "dependencies": {
    "gnim": "latest"
  },
  "prettier": {
    "semi": false,
    "printWidth": 100
  }
}
